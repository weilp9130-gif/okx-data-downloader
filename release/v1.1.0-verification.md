# Release v1.1.0 Verification

Release: v1.1.0
Commits: 257d528 (fix) + docs commit (docs: update README migration notes and release record)
Tag: v1.1.0

Tests: 193 passed
REST backfill: PASS
WebSocket realtime: PASS
PostgreSQL integration: PASS
Cold-start DB init: PASS
Latency Probe: PASS
Quality Report: PASS

Benchmark Environment:
  Python: 3.13.14
  OS: Windows 10 (10.0.19045) x64
  Proxy: 1 (system proxy 127.0.0.1:7888, Clash Verge; REST via proxy, no dynamic pool)
  Workers: 1 (sequential backfill)
  Database: PostgreSQL 16.14 + TimescaleDB 2.29.1
Result:
  rows/s: 303.2 (1440 rows in 4.75s, mark 1m x 1 day)
  pages/s: 2.95

Notes:
- Historical baseline 95 pages/s was a 32-IP dynamic proxy pool + high parallelism;
  this run is single-proxy sequential, bottleneck is proxy-link RTT (cold ~850ms,
  warm ~200-300ms). All requests succeeded, 0 errors — not a code regression.
- trades uniqueness validated by primary key (inst_id, trade_id, ts): 0 duplicates
  (546,186 rows total).
- latency_probe: machine clock ~1.56s behind OKX server; raw negative latencies are
  the documented clock-offset semantics, corrected values all non-negative.
- sync_continuous graceful shutdown verified via duration-based exit (writer flush,
  final summary, DB connection released).
- BOM fix applied to download_scope.toml parsing during functional testing
  (UTF-8 BOM made tomllib fail and silently fall back to default scope).

Date: 2026-08-18
